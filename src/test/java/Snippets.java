public class Snippets {

    /*
    * Input selector
    * $("input", withName().contains("number")).fill().with(mobileNumber)
    *
    * Select with method chaining
    * $(".user-number").$("input").fill().with(mobileNumber)
    *
    * Above selector also can be written like below
    * find(".user-number").find("input").fill().with(mobileNumber)
    *
    * Awesome nested selector
    * find(".pt-form").find("input").index(0).fill().with(mobileNumber)
     */
}
